local kpse = require('kpse') kpse.set_program_name('luatex') local lfs = require("lfs") local cacheDir = "./_markdown_main-poster" if lfs.isdir(cacheDir) == true then else assert(lfs.mkdir(cacheDir)) end local md = require("markdown") local convert = md.new({cacheDir = "./_markdown_main-poster", citations = true, definitionLists = true, footnotes = true, hashEnumerators = true, hybrid = true, smartEllipses = true, tightLists = false, } ) local input = assert(io.open("main-poster.markdown.in","r")):read("*a") print(convert(input:gsub("\r\n?", "\n")))
